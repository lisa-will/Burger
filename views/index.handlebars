<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="initial-scale = 1.0,maximum-scale = 1.0" />
    <!-- reset.css link -->
    <link rel="stylesheet" href="/assets/css/reset.css">
    <!-- bootstrap v 3.3x link -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <!-- google fonts link -->
    <link href="https://fonts.googleapis.com/css?family=Nunito|Open+Sans+Condensed:300|Pacifico" rel="stylesheet">
    <!-- style.css link --> 
    <link rel="stylesheet" href="/assets/css/burger_style.css" type="text/css" />
    <title>Eat-Da-Burger</title>
</head>

<body>
    {{{ body }}}
</body>

<div class="row">
    <div class="col-md-12">
        <h1 id="title">"Eat-Da-Burger!"</h1>
       <div class="animated bounce"></div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <h2>Burgers I <strong>WANT</strong> to eat:</h2>

        <ul>
            {{#each burgers}}{{#unless this.devoured}}
            <div class="newBurger">

                {{this.burger_name}}

                <form id="devour" action="burgers/update/{{this.id}}?_method_PUT" method="POST">
                <input type="hidden" name="devoured" value="1">
                <button type="submit">"Devour it!"</button>
                </form>
                </li>
            </div>
            {{/unless}}{{/each}}
            </ul>
            </div>

            <div class="col-md-12">
                <h2>Add a Burger!</h2>
                <form class="create-update-form" action="burgers/create">

                <div class="form-group">
                    <label for="burger_name" id="name">Burger:</label>
                    <input type="text" id="burger_name" name="burger_name">
                </div>

                <button type="submit" id="AddBtn">Add Burger</button>
                </form>
            </div>

                <div class="col-md-12">
                    <h2>Burgers I... <strong>DEVOURED!</strong> &#35;Yummy!</h2>

                    <ul>
                        {{#each burgers}} {{#if this.devoured}}
                        <div class="devouredBurger">
                            <li>
                                {{this.burger_name}}

                            </li>
                        </div>
                        {{/if}} {{/each}}
                    </ul>
                </div>
            

</html>


